<h1><%= @patient.name %></h1>
<h3>Age: <%= @patient.age %></h3>

<ul>
  <% @patient.appointments.each do |appt| %>
    <li><%= link_to "#{appt.formatted_date} - #{appt.doctor.name}", doctor_path(appt.doctor) %></li>
  <% end %>
</ul>
