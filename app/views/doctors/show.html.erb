<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= link_to "#{appt.formatted_date} - #{appt.patient.name}", patient_path(appt.patient) %></li>
  <% end %>
</ul>
